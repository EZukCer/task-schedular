[project]
name = "task-schedular"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "EZukCer", email = "erikzukov@proton.me" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
task-schedular = "task_schedular:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
